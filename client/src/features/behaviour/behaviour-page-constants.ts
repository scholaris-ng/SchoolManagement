import type { BehaviourTrait } from '@/types/behaviour';

/** Fixed option lists shared by `behaviour-page` and its parts. */

export const CATEGORIES: BehaviourTrait['category'][] = ['AFFECTIVE', 'PSYCHOMOTOR', 'SKILL', 'OTHER'];
